# URL del backend Fastify (Railway). Server-only — el browser nunca la ve (BFF).
# Local: http://localhost:3000  ·  Prod (Vercel): https://spa-mvp-production.up.railway.app
BACKEND_URL=http://localhost:3000
